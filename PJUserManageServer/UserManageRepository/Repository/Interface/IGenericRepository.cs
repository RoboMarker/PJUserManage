namespace MemberManage.Models.Repository.Interface
{
    public interface IGenericRepository
    {
       //Task<T> Get(int id);
       //Task<IEnumerable<T>> GetAll();
       //Task<int> Add(T entity);
       //
       //Task<int> Delete(int id);
       //
       //Task<int> Update(T entity);
    }
}
